# AWS Cognito — get values from Slack
VITE_COGNITO_USER_POOL_ID=
VITE_COGNITO_CLIENT_ID=
VITE_COGNITO_REGION=
VITE_COGNITO_DOMAIN=

# Backend API (default works for local Django dev server)
VITE_API_BASE_URL=http://localhost:8000

# GitHub App — get value from Slack
VITE_GITHUB_APP_NAME=

# Optional: set to your deployed URL to enable OAuth redirects in production
VITE_APP_URL=
