DEBUG=True
SECRET_KEY=django-insecure-replace-this-before-production
DATABASE_URL=postgres://clyro:clyro@localhost:5432/clyro_db
ALLOWED_HOSTS=localhost,127.0.0.1
CORS_ALLOWED_ORIGINS=http://localhost:5173

# AWS Cognito — values from: cd infrastructure/foundation && terraform output
COGNITO_REGION=ap-south-1
COGNITO_USER_POOL_ID=ap-south-1_RHY9si02J

# GitHub App — place github-app.pem in backend/ directory
GITHUB_APP_ID=3955174
GITHUB_APP_NAME=crylo-github
GITHUB_APP_PRIVATE_KEY_PATH=github-app.pem

# AWS — Bedrock + general AWS access
AWS_PROFILE=clyro
AWS_REGION=ap-south-1

# AgentCore runtimes — REQUIRED for Step 1 scan and Step 3 canvas
#   REPORECON  → Step 1 repo scan
#   REASONING  → Step 3 canvas prompts
#   IAC        → Step 4 CloudFormation generate/refine (validate works without it)
REPORECON_RUNTIME_ARN=arn:aws:bedrock-agentcore:ap-south-1:321613317660:runtime/CryloCanvas_RepoRecon-B2f80eGj0f
REASONING_RUNTIME_ARN=arn:aws:bedrock-agentcore:ap-south-1:321613317660:runtime/CryloCanvas_Reasoning-cZrFY185Gg
IAC_RUNTIME_ARN=
# AgentCore Memory for Step 3 canvas chat persistence (empty = persistence off)
AGENTCORE_MEMORY_ID=CryloCanvas_CryloCanvasMem-st2mFI3Zkp
