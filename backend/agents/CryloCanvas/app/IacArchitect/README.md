This is a project generated by the AgentCore CLI!

# IacArchitect (Step 4)

Authors and refines the single CloudFormation template Clyro provisions from. It
runs in two modes, selected by the invocation payload:

- `generate` — author a complete template from the deterministic **build spec**
  the Django backend assembles (`backend/app/provisioning/build_spec.py`).
- `refine` — apply one natural-language instruction to the current template.

In both modes it validates with `validate_cloudformation_template` (the `cfn`
gateway target) and self-corrects before returning. It receives only the distilled
build spec — never raw repo content — and holds no AWS write authority itself.

## Invocation payload

```json
{ "mode": "generate", "build_spec": { ... } }
{ "mode": "refine", "template": "<yaml>", "instruction": "make RDS multi-AZ",
  "build_spec": { ... }, "history": [ ... ] }
```

Returns `{"template": "<yaml>", "message": "<summary>"}`. The backend re-lints the
returned template (cfn-lint in-process) as the authoritative validation.

## Layout

`main.py` is the `@app.entrypoint`. `model/load.py` instantiates the Bedrock model;
`mcp_client/client.py` connects to the CryloCanvas gateway for the CFN + docs tools.

# Developing locally

`source .venv/bin/activate`, then `agentcore dev` starts a local server on
0.0.0.0:8080. Invoke with `agentcore invoke --dev --prompt-file payload.json`.

# Deployment

`agentcore deploy` deploys the runtime. Bind the `CryloCanvasGw` gateway so the
MCP URL is injected, then set `IAC_RUNTIME_ARN` in `backend/.env.local`.
